node_modules/
dist/
dist_installer/
.claude/
.env
softglaze.sqlite
softglaze.sqlite-journal
softglaze_profiles/
.DS_Store
chrome/
firefox/
fp-chromium/
# AdsPower SunBrowser kernel archive (9.68 GB). Reference copy only: those builds refuse
# to launch outside AdsPower, so they are not a usable engine here. Same class as the
# three above; electron-builder's files allowlist already keeps it out of installers.
sunbrowser/
# local dev test artifacts
dev-test.log
dev-test.err.log
dev-test.pid

# Ignore custom extensions and store packages
*.crx
*.xpi
*.zip
*.tar.gz
extensions/
src/renderer/public/extensions/
build/*
!build/entitlements.mac.plist
!build/afterPack.js
!build/installer.nsh
out/
.aidesigner/*
!.aidesigner/.gitkeep
